Datasets
========

.. currentmodule:: brainmass

.. note::

   **Placeholder** — authored in goal-13b / goal-13j. Reserves its navigation slot.

This page will document the ``brainmass.datasets`` registry
(``register_dataset``, ``list_datasets``, ``load_dataset``) and the bundled example data
(a small structural connectome, an example target signal, and a tiny task dataset) that
let the tutorials run with no external download.
